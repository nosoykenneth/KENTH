[pytest]
# Un solo comando para correr toda la suite del backend:  python -m pytest
testpaths = tests
addopts = -q
# Ruido conocido de dependencias (no son fallos del proyecto).
filterwarnings =
    ignore::DeprecationWarning
