# Moodle sirve pluginfile.php via PATH_INFO y usa .htaccess.
<Directory /var/www/html>
    Options FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
AcceptPathInfo On
ServerName localhost
